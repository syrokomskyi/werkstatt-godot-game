# extract.config.yaml — repo-extract standalone package export for @warpgogol/werkstatt-godot-game
# Extracts packages/werkstatt-godot-game from the werkstatt monorepo into a standalone private repo.
# RFC-0773: Engine publication pipeline via repo-extract to private npm.

projectDir: packages/werkstatt-godot-game
destName: werkstatt-godot-game
standalone: true
versionBump: minor

workspacePrefixes:
  - "@warpgogol/"
stripScopes:
  - "@warpgogol/"
preservePackages:
  - "@warpgogol/werkstatt-godot-game"
  - "@warpgogol/werkstatt-engine"
  - "@warpgogol/werkstatt-shared"
packageManager: pnpm

ci:
  provider: github-actions
  publish: true
  nodeVersion: 24

ignoreDirs:
  - .turbo
  - .agents
  - node_modules
  - tmp-*

excludePathSegments:
  - src/tests

postProcess:
  - action: delete
    path: AGENTS.md

git:
  remote: git@github.com:syrokomskyi/werkstatt-godot-game.git
  autoPush: true
